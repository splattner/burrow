apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: krt-server
  namespace: default
subjects:
  - kind: ServiceAccount
    name: krt-server
    namespace: default
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: krt-server
