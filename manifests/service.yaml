apiVersion: v1
kind: Service
metadata:
  name: krt-server
  namespace: default
  labels:
    app: krt-server
spec:
  selector:
    app: krt-server
  ports:
    - name: http
      port: 80
      targetPort: 8080
